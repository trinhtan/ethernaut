{
  "scripts": {
    "test": "npm run compile && npx mocha --timeout 5000 --exit --recursive test",
    "fallback": "npm run compile && npx mocha --timeout 5000 --exit test/fallback/fallback.challenge.js",
    "fallout": "npm run compile && npx mocha --timeout 5000 --exit test/fallout/fallout.challenge.js",
    "coin-flip": "npm run compile && npx mocha --timeout 5000 --exit test/coin-flip/coin-flip.challenge.js",
    "telephone": "npm run compile && npx mocha --timeout 5000 --exit test/telephone/telephone.challenge.js",
    "token": "npm run compile && npx mocha --timeout 5000 --exit test/token/token.challenge.js",
    "force": "npm run compile && npx mocha --timeout 5000 --exit test/force/force.challenge.js",
    "king": "npm run compile && npx mocha --timeout 5000 --exit test/king/king.challenge.js",
    "reentrance": "npm run compile && npx mocha --timeout 5000 --exit test/reentrance/reentrance.challenge.js",
    "compile": "npx buidler compile"
  },
  "license": "MIT",
  "dependencies": {
    "@nomiclabs/buidler": "1.4.7",
    "@openzeppelin/contracts": "2.3.0",
    "@openzeppelin/test-environment": "0.1.3",
    "@openzeppelin/test-helpers": "0.5.5",
    "mocha": "7.1.1"
  }
}